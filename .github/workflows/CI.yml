name: CI (retired)
on:
  workflow_call:
# push/pull_requestトリガーは削除
jobs:
  backend:
    uses: ./.github/workflows/backend-tests.yml
    with:
      mode: integration
      concierge_throttle: "1000/min"
