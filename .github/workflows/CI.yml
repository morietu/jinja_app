name: CI (delegate to backend-tests)
on:
  push:
  pull_request:
jobs:
  backend:
    uses: ./.github/workflows/backend-tests.yml
    with:
      mode: integration
      concierge_throttle: "1000/min"
