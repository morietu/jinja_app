name: CI
on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgis/postgis:16-3.4
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: jinja_db
        ports:
          - "5432:5432"
        options: >-
          --health-cmd="pg_isready -U postgres -d jinja_db"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=20

    env:
      PYTHONPATH: backend
      DJANGO_SETTINGS_MODULE: shrine_project.settings
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/jinja_db
      USE_GIS: "1"
      USE_SQLITE: "0"
      IS_PYTEST: "1"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Upgrade pip
        run: python -m pip install -U pip

      - name: Install Python deps
        working-directory: backend
        run: pip install -r requirements.txt

      # 任意：GDAL/GEOSが必要なら（いまはGDAL必須テストはskip設計なので省略可）
      # - name: (optional) Install GDAL libs
      #   run: |
      #     sudo apt-get update
      #     sudo apt-get install -y gdal-bin libgdal-dev libgeos-dev

      - name: Wait for DB
        run: |
          for i in {1..60}; do
            pg_isready -h localhost -p 5432 -U postgres -d jinja_db && break
            sleep 1
          done

      - name: Migrate
        working-directory: backend
        run: python -m django migrate --noinput

      - name: Run tests
        working-directory: backend
        run: pytest -q
