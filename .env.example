# ========= App toggles =========
DEBUG=1
TESTING=1
AUTO_GEOCODE_ON_SAVE=0

# ========= Provider switches (無料系を既定) =========
USE_GOOGLE=false

# --- Free providers (既定で有効) ---
NOMINATIM_BASE=https://nominatim.openstreetmap.org
NOMINATIM_EMAIL=your-contact@example.com   # OSM礼儀として必須
ORS_BASE=https://api.openrouteservice.org
ORS_KEY=__REPLACE_ME__

# --- Google providers (使う時だけ入れる) ---
GOOGLE_MAPS_API_KEY=__REPLACE_ME__
GOOGLE_PLACES_API_KEY=__REPLACE_ME__

# ========= LLM / Concierge =========
USE_LLM_CONCIERGE=0
LLM_PROVIDER=openai
OPENAI_API_KEY=__REPLACE_ME__
LLM_MODEL=gpt-4o-mini
LLM_TIMEOUT_MS=6000
LLM_TEMPERATURE=0.2
LLM_MAX_TOKENS=800
LLM_FORCE_CHAT=1
LLM_FORCE_JSON=1
LLM_RETRIES=2
LLM_BACKOFF_S=0.5

# ========= Throttle (DRF) =========
DISABLE_THROTTLE=0
THROTTLE_ANON=60/min
THROTTLE_USER=120/min
THROTTLE_CONCIERGE=60/min
THROTTLE_PLACES=30/min
THROTTLE_PLACES_NEARBY=30/min
THROTTLE_SHRINES=60/min

# ========= Cache =========
CACHE_BACKEND=locmem   # or "redis"
# REDIS_URL=redis://localhost:6379/0

# ========= HOSTS =========
ALLOWED_HOSTS=localhost,127.0.0.1

# ========= DB プロファイル (どちらか一方だけ使う) =========

# --- ① PostGIS（Docker/Postgres） ---
USE_GIS=1
USE_SQLITE=0
DB_NAME=jinja_db
DB_USER=admin
DB_PASSWORD=jdb50515
DB_HOST=127.0.0.1
DB_PORT=5432
DATABASE_URL=postgis://admin:jdb50515@127.0.0.1:5432/jinja_db

# --- ② Spatialite（ローカル簡易・テスト用） ---
# USE_GIS=1
# USE_SQLITE=1
# SPATIALITE_LIBRARY_PATH=/opt/homebrew/opt/libspatialite/lib/mod_spatialite.dylib
# DATABASE_URL=sqlite:///../db.sqlite3
