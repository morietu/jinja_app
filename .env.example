# Local testing (Spatialite)
TESTING=1
AUTO_GEOCODE_ON_SAVE=0
SPATIALITE_LIBRARY_PATH=$(brew --prefix libspatialite)/lib/mod_spatialite.dylib

# DB (CI/Productionは別管理)
USE_GIS=1
USE_SQLITE=0

DB_NAME=jinja_db          # ← ★ 統一！
DB_USER=admin
DB_PASSWORD=jdb50515
DB_HOST=127.0.0.1         # ← macローカル用
DB_PORT=5432

# LLM (必要なら)
# OPENAI_API_KEY=sk-...
